<!DOCTYPE html>
<html>
  [/- include('head'); /]
  <body>
    <div class="w-full flex flex-col justify-center items-center">
      <h1 class="w-full text-emerald-500 font-bold text-center text-4xl">[/= title /]</h1>
      <div class="w-[50%] relative mt-20 flex flex-row justify-center items-center">
      <h3 class="text-center font-bold text-3xl text-gray-500 underline">게시글</h3>
      <a href="/board/write" class="absolute right-0 top-0 px-4 py-2 bg-emerald-500 text-white rounded-md">글쓰기</a>
      </div>
    <ul class="w-full flex flex-col justify-center items-center">
      [/ userPosts?.map((post) => { /]
        <li class="mt-4 border py-2 px-4 flex flex-row rounded-md items-center" style="width: 50%;">
          <a href="/board/[/= post.id /]" class="self-start cursor-pointer w-full text-xl text-black font-bold">[/= post.title /]</a>
          <span style="min-width: 120px;" class="inline-block self-end text-sm text-gray-400">[/= post.createdAt /]</span>
        </li>
      [/ }) /]
    </ul>
  </div>

  </body>
</html>